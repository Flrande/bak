{
  "name": "@bak/extension",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "tsup src/background.ts src/content.ts src/popup.ts --format iife --out-dir dist --clean && node scripts/copy-assets.mjs",
    "dev": "tsup src/background.ts src/content.ts src/popup.ts --format iife --out-dir dist --watch && node scripts/copy-assets.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src --ext .ts"
  },
  "dependencies": {
    "@bak/protocol": "workspace:*"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.14",
    "tsup": "^8.5.0"
  }
}
